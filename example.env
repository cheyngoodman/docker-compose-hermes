HERMES_UID=1000
HERMES_GID=1000
HERMES_DASHBOARD_USERNAME=admin
HERMES_DASHBOARD_PASSWORD=changeme
HERMES_DASHBOARD_SECRET=<32+ random bytes; openssl rand -base64 32>

# Hermes WebUI (in-container web interface at :8787)
# Set to 0 to disable; enabled by default.
HERMES_WEBUI_ENABLED=1
# Password for the WebUI login page.
# REQUIRED to enable the WebUI — on first boot the container auto-clones
# and configures hermes-webui automatically. Leave empty to skip WebUI entirely.
# Generate:  openssl rand -base64 18
HERMES_WEBUI_PASSWORD=
